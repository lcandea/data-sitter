zsh -c 'python -m venv .venv && source .venv/bin/activate && pip install --upgrade pip && pip install -r requirements.txt'
# sudo chown -R vscode:vscode /workspace/ui/node_modules
# zsh -c 'cd ui && npm install'
